services:
  stroopwafel:
    image: ghcr.io/joeblack2k/stroopwafel-social-media-manager:${IMAGE_TAG:-latest}
    container_name: stroopwafel
    restart: unless-stopped
    pull_policy: always
    ports:
      - "8080:8080"
    volumes:
      - /opt/stacks/tools/stroopwafel:/data
